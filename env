// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    appInsights: {
      instrumentationKey: 'XXXXXXXXXXXXXXXXXXXXXXXXXXX',
      applicationName: 'Depot UI',
      autoTrackPageVisitTime: true
    },
    baseURL: 'https://apim-depot-dev-poc.azure-api.net',
    mockBaseURL: 'http://localhost:3000',
    msal: {
      tenantId: '7cacbdfd-ebad-46c0-8d1e-b7058ce44173',
      clientId: 'bfd069bd-1c27-4215-a689-29d493e19e59',
      backendScope: ['api://d17c3d94-6a55-4b8f-8b4e-ff7c04d3714d/access_as_user'],
      postLogoutRedirectUri: '/',
      redirectUri: window.location.origin,
      mainWindowRedirectUri: '/',
      navigateToLoginRequestUrl: true
    },
    production: false
  };
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
  
